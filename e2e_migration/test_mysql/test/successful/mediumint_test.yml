query: |
  CALL migrate.mysql('SELECT mediumint_col FROM dummy_table', {
      host: 'localhost',
      port: 3306,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.mediumint_col AS mediumint_col

exception: true
