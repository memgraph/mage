query: |
  CALL migrate.mysql('dummy_table', {
      host: 'localhost',
      port: 3306,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.id AS id, row.mediumint_col AS mediumint_col
  ORDER BY row.id

output:
  - id: 1
    mediumint_col: 8388607

exception: false
