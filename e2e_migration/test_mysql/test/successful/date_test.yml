query: |
  CALL migrate.mysql('SELECT date_col FROM dummy_table', {
      host: 'localhost',
      port: 3306,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.date_col AS date_col

exception: true
