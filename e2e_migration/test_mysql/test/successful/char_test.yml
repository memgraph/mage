query: |
  CALL migrate.mysql('dummy_table', {
      host: 'localhost',
      port: 3306,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.id AS id, row.char_col AS char_col
  ORDER BY row.id

output:
  - id: 1
    char_col: "char_test"

exception: false
