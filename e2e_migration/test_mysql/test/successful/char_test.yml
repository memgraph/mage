query: |
  CALL migrate.mysql('SELECT char_col FROM dummy_table', {
      host: 'localhost',
      port: 3306,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.char_col AS char_col

exception: true
