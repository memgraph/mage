query: |
  CALL migrate.mysql('dummy_table', {
      host: 'localhost',
      port: 3306,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.id AS id, row.varchar_col AS varchar_col
  ORDER BY row.id

output:
  - id: 1
    varchar_col: "varchar_test"

exception: false
