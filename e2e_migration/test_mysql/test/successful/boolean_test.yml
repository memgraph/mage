query: |
  CALL migrate.mysql('SELECT boolean_col FROM dummy_table', {
      host: 'localhost',
      port: 3306,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.boolean_col AS boolean_col

exception: true
