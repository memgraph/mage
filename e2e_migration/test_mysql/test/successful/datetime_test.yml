query: |
  CALL migrate.mysql('dummy_table', {
      host: 'localhost',
      port: 3306,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.id AS id, row.datetime_col AS datetime_col
  ORDER BY row.id

output:
  - id: 1
    datetime_col: "2023-12-25 10:30:45"

exception: false
