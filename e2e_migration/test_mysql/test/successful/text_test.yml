query: |
  CALL migrate.mysql('dummy_table', {
      host: 'localhost',
      port: 3306,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.id AS id, row.text_col AS text_col
  ORDER BY row.id

output:
  - id: 1
    text_col: "text_data"

exception: false
