query: |
  CALL migrate.mysql('dummy_table', {
      host: 'localhost',
      port: 3306,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.id AS id, row.double_col AS double_col
  ORDER BY row.id

output:
  - id: 1
    double_col: 4.56

exception: false
