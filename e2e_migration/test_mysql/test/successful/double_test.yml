query: |
  CALL migrate.mysql('SELECT double_col FROM dummy_table', {
      host: 'localhost',
      port: 3306,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.double_col AS double_col

exception: true
