query: |
  CALL migrate.mysql('dummy_table', {
      host: 'localhost',
      port: 3306,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.id AS id, row.smallint_col AS smallint_col
  ORDER BY row.id

output:
  - id: 1
    smallint_col: 32767

exception: false
