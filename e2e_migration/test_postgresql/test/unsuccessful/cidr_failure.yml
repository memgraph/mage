query: |
  CALL migrate.postgresql('SELECT cidr_col FROM dummy_table', {
      host: 'localhost',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.cidr_col AS cidr_col

exception: true
