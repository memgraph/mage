query: |
  CALL migrate.postgresql('SELECT inet_col FROM dummy_table', {
      host: 'localhost',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.inet_col AS inet_col

exception: true
