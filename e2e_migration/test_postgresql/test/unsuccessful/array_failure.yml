query: |
  CALL migrate.postgresql('SELECT array_col FROM dummy_table', {
      host: 'localhost',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.array_col AS array_col

exception: true
