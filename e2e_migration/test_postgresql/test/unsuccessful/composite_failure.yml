query: |
  CALL migrate.postgresql('SELECT composite_col FROM dummy_table', {
      host: 'e2e_migration_postgresql',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN valueType(row.composite_col) AS composite_col_type, toString(row.composite_col) AS composite_col

exception: true
