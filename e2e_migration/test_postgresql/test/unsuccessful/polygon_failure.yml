query: |
  CALL migrate.postgresql('SELECT polygon_col FROM dummy_table', {
      host: 'localhost',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.polygon_col AS polygon_col

exception: true
