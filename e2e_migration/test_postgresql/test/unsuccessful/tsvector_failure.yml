query: |
  CALL migrate.postgresql('SELECT tsvector_col FROM dummy_table', {
      host: 'localhost',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.tsvector_col AS tsvector_col

exception: true
