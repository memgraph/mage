query: |
  CALL migrate.postgresql('SELECT range_col FROM dummy_table', {
      host: 'e2e_migration_postgresql',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN valueType(row.range_col) AS range_col_type, toString(row.range_col) AS range_col

exception: true
