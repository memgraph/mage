query: |
  CALL migrate.postgresql('SELECT json_col FROM dummy_table', {
      host: 'localhost',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.json_col AS json_col

exception: true
