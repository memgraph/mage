query: |
  CALL migrate.postgresql('dummy_table', {
      host: 'localhost',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.id AS id, row.double_precision_col AS double_precision_col
  ORDER BY row.id

output:
  - id: 1
    double_precision_col: 4.56

exception: false
