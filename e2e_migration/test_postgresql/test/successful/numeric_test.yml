query: |
  CALL migrate.postgresql('SELECT numeric_col FROM dummy_table', {
      host: 'localhost',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.numeric_col AS numeric_col

exception: true
