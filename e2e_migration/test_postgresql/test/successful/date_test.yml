query: |
  CALL migrate.postgresql('dummy_table', {
      host: 'localhost',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.id AS id, row.date_col AS date_col
  ORDER BY row.id

output:
  - id: 1
    date_col: "2023-12-25"

exception: false
