query: |
  CALL migrate.postgresql('SELECT uuid_col FROM dummy_table', {
      host: 'localhost',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.uuid_col AS uuid_col

exception: true
