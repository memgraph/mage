query: |
  CALL migrate.postgresql('SELECT interval_col FROM dummy_table', {
      host: 'e2e_migration_postgresql',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN valueType(row.interval_col) AS interval_col_type, toString(row.interval_col) AS interval_col

output:
  - interval_col_type: 'DURATION'
    interval_col: 'P0DT23H39M50.065408S'
