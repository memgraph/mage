query: |
  CALL migrate.postgresql('SELECT interval_col FROM dummy_table', {
      host: 'localhost',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.interval_col AS interval_col

exception: true
