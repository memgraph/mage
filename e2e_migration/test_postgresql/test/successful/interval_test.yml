query: |
  CALL migrate.postgresql('dummy_table', {
      host: 'localhost',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.id AS id, row.interval_col AS interval_col
  ORDER BY row.id

output:
  - id: 1
    interval_col: "1 day 02:03:00"

exception: false
