query: |
  CALL migrate.postgresql('SELECT text_col FROM dummy_table', {
      host: 'localhost',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.text_col AS text_col

exception: true
