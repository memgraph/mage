query: |
  CALL migrate.postgresql('SELECT jsonb_col FROM dummy_table', {
      host: 'e2e_migration_postgresql',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN valueType(row.jsonb_col) AS jsonb_col_type, row.jsonb_col AS jsonb_col

output:
  - jsonb_col_type: 'MAP'
    jsonb_col: {'key': 'value', 'number': 42}
