query: |
  CALL migrate.postgresql('SELECT money_col FROM dummy_table', {
      host: 'localhost',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.money_col AS money_col

exception: true
