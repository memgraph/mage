query: |
  CALL migrate.postgresql('SELECT real_col FROM dummy_table', {
      host: 'localhost',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.real_col AS real_col

exception: true
