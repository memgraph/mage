query: |
  CALL migrate.postgresql('dummy_table', {
      host: 'localhost',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.id AS id, row.real_col AS real_col
  ORDER BY row.id

output:
  - id: 1
    real_col: 1.23

exception: false
