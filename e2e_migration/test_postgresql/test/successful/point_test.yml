query: |
  CALL migrate.postgresql('SELECT id, point_col FROM dummy_table', {
      host: 'e2e_migration_postgresql',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN valueType(row.point_col) AS point_col_type, toString(row.point_col) AS point_col

output:
  - point_col_type: 'STRING'
    point_col: '(1,2)'
