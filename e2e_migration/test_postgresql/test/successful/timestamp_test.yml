query: |
  CALL migrate.postgresql('SELECT timestamp_col FROM dummy_table', {
      host: 'localhost',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.timestamp_col AS timestamp_col

exception: true
