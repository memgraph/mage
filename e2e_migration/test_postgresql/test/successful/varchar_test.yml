query: |
  CALL migrate.postgresql('SELECT varchar_col FROM dummy_table', {
      host: 'localhost',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.varchar_col AS varchar_col

exception: true
