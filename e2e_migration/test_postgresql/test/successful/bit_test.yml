query: |
  CALL migrate.postgresql('SELECT bit_col FROM dummy_table', {
      host: 'localhost',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.bit_col AS bit_col

exception: true
