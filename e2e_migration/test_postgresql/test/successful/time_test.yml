query: |
  CALL migrate.postgresql('dummy_table', {
      host: 'localhost',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.id AS id, row.time_col AS time_col
  ORDER BY row.id

output:
  - id: 1
    time_col: "10:30:45"

exception: false
