query: |
  CALL migrate.postgresql('SELECT time_col FROM dummy_table', {
      host: 'localhost',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.time_col AS time_col

exception: true
