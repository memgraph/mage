query: |
  CALL migrate.postgresql('SELECT smallint_col FROM dummy_table', {
      host: 'localhost',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN row.smallint_col AS smallint_col

exception: true
