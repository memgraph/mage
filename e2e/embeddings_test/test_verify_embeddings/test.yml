query: >
    MATCH (n:Document)
    RETURN n.title AS title, 
           size(n.embeddings) AS embedding_size,
           n.embeddings IS NOT NULL AS has_embeddings;

output:
    - title: "Magnetospheric Field Line Resonances"
      embedding_size: 384
      has_embeddings: true
