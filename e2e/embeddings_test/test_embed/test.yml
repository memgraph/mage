query: >
  WITH {device: "cpu"} AS configuration 
  CALL embeddings.text(["Hello, world!"], configuration) 
  YIELD success, embeddings 
  RETURN size(embeddings) as embeddings_num, size(embeddings[0]) as embedding_size;

output:
  - embeddings_num: 1
    embedding_size: 384
