query: >
        CALL node_classification.set_model_parameters({path_to_model: '../pytorch_models/model1'}) YIELD *;

output:
        - in_channels: 34
          out_channels: 4
          num_samples: 34
          path: ../pytorch_models/model1

query: >
        CALL node_classification.save_model() YIELD *;

output:
        - path: ../pytorch_models/model1


query: >
        CALL node_classification.load_model() YIELD *;

output:
        - path: ../pytorch_models/model1

query: >
        CALL node_classification.reset() YIELD *;

output: []