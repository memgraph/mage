query: >
  CALL link_prediction.set_model_parameters({hidden_features_size: [3, 2], split_ratio: 1.0, target_relation: "CITES"}) YIELD *
  CALL link_prediction.train()
  YIELD training_results, validation_results
  RETURN validation_results;

output:
  - validation_results: []
