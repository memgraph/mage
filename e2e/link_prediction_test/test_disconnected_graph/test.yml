query: >
      CALL link_prediction.set_model_parameters({hidden_features_size: [3, 2], split_ratio: 0.6}) YIELD *
      CALL link_prediction.train()
      YIELD *
      RETURN 0 as check;

output: 
    - check: 0