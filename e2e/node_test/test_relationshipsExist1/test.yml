query: >
    MATCH (p1:Person)
    WHERE p1.name IN ["Michael"]
    CALL node.relationships_exist(p1, ["KNOWS>","FOLLOWS"]) YIELD result RETURN result;
output:
  - result: {"FOLLOWS": true,"KNOWS>": true}
